# 서버 재가동 계획

## 🎯 목표
- 백엔드(Spring Boot, 8080 포트)와 프론트엔드(Vite, 5173 포트) 서버를 각각 성공적으로 구동합니다.

## 🛠 작업 단계

### Phase 1: 백엔드 서버 구동
- [ ] `backend` 디렉토리로 이동하여 `./gradlew bootRun` 명령을 실행합니다.
- [ ] `backend_run.log`를 통해 서버가 성공적으로 8080 포트에서 시작되었는지 확인합니다.

### Phase 2: 프론트엔드 서버 구동
- [ ] `frontend` 디렉토리로 이동하여 `npm run dev` 명령을 실행합니다.
- [ ] 프론트엔드가 5173 포트에서 정상적으로 호스팅되는지 확인합니다.

### Phase 3: 최종 상태 확인
- [ ] 두 서버의 로그를 모니터링하여 오류 발생 여부를 최종 점검합니다.
- [ ] `docs/frontend-modification-history.md`에 서버 재가동 기록을 남깁니다.

---
[✅ 규칙을 잘 수행했습니다.]
