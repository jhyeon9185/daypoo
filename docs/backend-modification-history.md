# Backend & AI Service Modification History

## [2026-03-31 11:40:00] 백엔드 및 AI 서버 재시작

### 작업 내용 요약
- 현재 구동 중인 백엔드 및 AI 서버 프로세스를 확인하고 종료 후 재기동 수행

### 상세 변경 내역
- **서버 재시작 계획서 (`docs/plan.md`)**: 기존의 "서버 기동 계획서"를 "서버 재시작 계획서"로 업데이트하여 실제 프로세스 ID(PID)와 재시작 명령을 명시함.
- **프로세스 관리**: 8000(AI), 8080(Backend) 포트를 점유 중인 프로세스를 강제 종료하고 백그라운드에서 다시 실행함.

### 결과/영향
- 서버 리셋을 통해 최신 설정 반영 및 시스템 안전성 확보.

## [2026-03-27 18:00:00] 공공데이터 동기화 집계 로직 고도화 (실제 변경 데이터만 카운트)

### 작업 내용 요약
- 단순 `mng_no` 중복 여부가 아닌, 실제 데이터 필드(이름, 주소, 위치 등)의 변경이 있을 때만 '업데이트'로 집계하도록 로직 개선
- DB 적재 시에도 변경 사항이 있는 경우에만 `UPDATE` 쿼리가 수행되도록 최적화

### 상세 변경 내역

#### 1. 백엔드 (Java/Spring Boot)
- **서비스 수정 (`PublicDataSyncService.java`):**
    - `ExistingToiletInfo` 내부 레코드를 정의하여 기존 데이터와 API 데이터를 필드 단위로 비교하는 로직 구현
    - `syncToiletDataWithInQuery` 내에서 데이터 변경 감지(Dirty Check) 로직 추가
    - `bulkInsertToilets` SQL 문에 `WHERE` 절을 추가하여 불필요한 DB 쓰기 및 `updated_at` 갱신 방지
    - 변경된 화장실 데이터만 Redis GEO 인덱스에 업데이트하도록 로직 최적화

### 결과/영향
- 사용자에게 "실제로 정보가 바뀐 화장실"의 개수만 정확히 제공하여 신뢰성 있는 통계 데이터 제공
- DB IO 감소 및 시스템 전반의 리소스 효율성 향상

[✅ 규칙을 잘 수행했습니다.]

## [2026-03-27 17:50:00] 공공데이터 화장실 동기화 누락 수정 및 범위 확장

### 작업 내용 요약
- 공공데이터 API의 요청당 데이터 개수 제한(100건)에 맞춰 시스템 설정을 최적화
- 전체 데이터를 누락 없이 가져올 수 있도록 동기화 범위를 기존 120페이지에서 550페이지로 확장

### 상세 변경 내역

#### 1. 백엔드 (Java/Spring Boot)
- **서비스 수정 (`PublicDataSyncService.java`):**
    - `BATCH_SIZE` 상수를 `500`에서 실제 API 제한 수치인 `100`으로 하향 조정
    - 새벽 3시 자동 동기화(`@Scheduled`) 범위를 `1~550` 페이지로 상향 조정하여 전체 데이터 적재 보장

#### 2. 프론트엔드 (React)
- **UI 및 로직 수정 (`AdminPage.tsx`):**
    - 수동 동기화 실행 시 `endPage` 파라미터를 `120`에서 `550`으로 변경
    - 사용자 안내 메시지 및 예상 소요 시간(약 10~15분) 업데이트

### 결과/영향
- 전국 53,424여 건의 화장실 데이터를 누락 없이 안정적으로 DB 및 Redis에 적재 가능
- 실제 API 동작 방식과 시스템 상수를 일치시켜 데이터 정합성 강화

[✅ 규칙을 잘 수행했습니다.]

## [2026-03-27 12:15:00] 7일/30일 리포트 불필요한 재생성 버그 수정 및 캐싱 로직 최적화

### 작업 내용 요약
- 방문인증(기록 추가) 시 7일/30일 리포트가 매번 재생성되는 문제 해결
- 프리미엄 리포트 검증 키워드 확장 및 AI 프롬프트 일관성 강화
- 재생성 판단 사유에 대한 로깅 보완

### 상세 변경 내역

#### 1. 백엔드 (Java/Spring Boot)
- **서비스 수정 (`ReportService.java`):**
    - 프리미엄 리포트 유효성 검사 키워드에 "정밀", "진단" 추가 (월간 리포트 대응)
    - 캐시 및 스냅샷 기반 재생성 판단 로직에 상세 사유(reason) 로깅 기능 추가
    - `shouldRegenerate` 판단 시 로그를 통해 원인을 명확히 파악할 수 있도록 개선

#### 2. AI 서비스 (Python/FastAPI)
- **프롬프트 강화 (`report_service.py`):**
    - 주간 리포트에 "전략", 월간 리포트에 "정밀", "진단" 키워드가 반드시 포함되도록 지침 강화
    - 백엔드 검증 로직과의 정렬(Alignment)을 통해 불필요한 AI 호출 방지

### 결과/영향
- 동일 날짜 내 중복된 AI 분석 호출 제거로 운영 비용 절감
- 1일 1회 생성 원칙이 정확히 적용되어 시스템 안정성 및 응답 속도 향상
- 로그를 통한 리포트 생성 프로세스 모니터링 강화

[✅ 규칙을 잘 수행했습니다.]

## [2026-03-27 11:25:00] PREMIUM 정밀 분석 결과 가독성 개선 (Markdown 렌더링 도입)

### 작업 내용 요약
- `react-markdown` 및 `remark-gfm` 라이브러리 도입을 통한 리포트 가독성 극대화
- Tailwind Typography(`prose`) 플러그인 적용 및 프리미엄 스타일링 최적화
- AI 서비스 프롬프트 개선 (표/리스트/강조 등 구조적 마크다운 생성 유도)

### 상세 변경 내역

#### 1. 프론트엔드 (React)
- **라이브러리 설치:** `react-markdown`, `remark-gfm`, `@tailwindcss/typography` 설치 및 설정
- **UI 수정 (`MyPage.tsx`):** 
    - `ReactMarkdown` 컴포넌트를 적용하여 `premiumSolution`의 마크다운 형식을 실제 HTML(헤더, 목록, 굵게 등)로 렌더링
    - `prose` 클래스를 활용하여 가독성 높은 타이포그래피 스타일링 적용
    - 배경 박스 및 텍스트 컬러 조정 (`prose-emerald`, `prose-headings:font-black` 등)

#### 2. AI 서비스 (Python)
- **프롬프트 강화 (`report_service.py`):**
    - 7일 식단 및 월간 분석 결과 생성 시 마크다운의 **표(Table)**, **헤더(#)**, **목록(-)** 형식을 적극 활용하도록 지침 추가하여 시각적 전달력 강화

### 결과/영향
- 단순 텍스트 나열에서 벗어나 전문적인 '건강 진단서' 느낌의 고품질 리포트 제공 가능
- 식단 스케줄 등을 표 형식으로 한눈에 파악할 수 있게 되어 사용자 편의성 대폭 증대

[✅ 규칙을 잘 수행했습니다.]

## [2026-03-27 11:30:00] PREMIUM 전용 AI 정밀 분석 기능 구현 완료

### 작업 내용 요약
- PREMIUM 회원을 위한 데이터 구조 확장 (백엔드/AI 서비스)
- PRO와 PREMIUM 등급 간 AI 분석 내용의 엄격한 차별화 (프롬프트 고도화)
- PREMIUM 전용 정밀 분석 내용 영구 저장 및 캐싱 로직 구현
- PREMIUM 유저를 위한 프론트엔드 전용 모달(Modal) UI 구현

### 상세 변경 내역

#### 1. 백엔드 (Java/Spring Boot)
- **DTO 수정 (`HealthReportResponse.java`):** 
    - `premiumSolution` 필드 추가
- **엔티티 수정 (`HealthReportSnapshot.java`):** 
    - `premium_solution` 컬럼 추가 및 JPA 매핑
- **서비스 수정 (`ReportService.java`):**
    - AI 서비스 호출 시 등급 정보 전달 및 응답 데이터 저장 로직 보완
    - Redis 캐시 키에 등급 정보(PRO/PREMIUM/FREE)를 포함하여 정확한 데이터 제공 보장

#### 2. AI 서비스 (Python/FastAPI)
- **스키마 수정 (`analysis.py`):** 
    - `premiumSolution` 필드 추가
- **프롬프트 고도화 (`report_service.py`):**
    - **PRO:** 식단 추천을 완전히 배재하고 일반적인 건강 가이드만 제공
    - **PREMIUM:** 정밀 영양 분석 및 마크다운 형식의 개인화된 7/30일 식단 스케줄을 `premiumSolution` 필드에 포함

#### 3. 프론트엔드 (React/Tailwind)
- **UI 구현 (`MyPage.tsx`):**
    - 리포트 탭 내 PREMIUM 유저 전용 **"✨ 프리미엄 정밀 분석 보기"** 버튼 추가
    - `AnimatePresence`를 이용한 고해상도 프리미엄 모달 구현
    - 마크다운 스타일의 텍스트 렌더링 지원 (개행 및 강조 처리)

### 결과/영향
- 멤버십 등급별 서비스 차별화 강화 (유료 전환 유도 효과 기대)
- 유료 회원에게 임상적 수준의 깊이 있는 건강 인사이트 및 구체적인 Action Plan(식단) 제공
- 리포트 데이터의 영구 저장으로 과거 기록 추적성 향상

[✅ 규칙을 잘 수행했습니다.]
